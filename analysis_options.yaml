include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Treat unused imports as errors so CI catches dead code early.
    unused_import: error

linter:
  rules:
    prefer_const_constructors: true
    prefer_const_declarations: true
    avoid_print: true
